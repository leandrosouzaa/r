# Aula 3 - Workspace (Básico)

# Criar variável x

x <- 0

# Criando Outra variável
variavelWorkSpace <- "exemplo"

# Listar os objetos no workspace
ls()

# Apagar objeto do Workspace
# rm(nome da variável)

# Apagar Todos rm (list = ls())
rm(list = ls())

ls()